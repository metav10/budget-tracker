import { InitialState } from '../lib/interfaces'

const initialState: InitialState = {
    user: {
        _id: '',
        username: '',
        budget: 0,
    },
    expenses: [],
}

export default initialState
